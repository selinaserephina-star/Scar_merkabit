# BRIEF — STONE W: THE THIRD SHADOW (triality under the roof)

**Staged 2026-09-01 on Selina's "do it" (the triality work-item computed
ahead inside the adopted joint target; proposed to IB in the ROOF cover, ask
#3). Locked before code (`BRIEF_STONE_W_LOCK.sha256`). Deviations = dated
AMENDMENT, sha'd before any reveal.**

## Question

SM-030: the minimal-found roof R = W⁺(E₈) has R/⟨−1⟩ of order 174,182,400 =
|O₈⁺(2)| — identified by ORDER ONLY. O₈⁺(2) is the home of triality: its
outer S₃ permutes THREE families of 8-dimensional structure (vector, spin⁺,
spin⁻), and correspondingly three conjugacy classes of Sp₆(2)-type
subgroups. Stone U/V found two: the vector-type C̄ (body tower, split lifts)
and the spin-type K_spin (shadow tower, Schur lifts). Stone W asks: **is
the identification real (beyond order), and are the two shadows actually
three?**

## Bars

- **WB1 (the identification, beyond order):** R/⟨−1⟩ ≅ the SIMPLE O₈⁺(2),
  via the natural module: R acts on L/2L = (𝔽₂⁸, q) with kernel exactly
  ⟨−1⟩ (verify −1 ∈ R and kernel size); the image lands inside the full
  orthogonal group of q; compute the **Dickson invariant** (rank(g−1) mod 2)
  of every generator's image — all even ⟺ image ⊆ Ω(q) = the simple group;
  order match forces equality. PASS = R/± ≅ O₈⁺(2) as the simple group,
  [C] not order-only.
- **WB2 (the geometry of three):** build the triality playground — the 135
  singular points of q and the maximal totally singular 4-subspaces
  (expect 270, in TWO families of 135, same family ⟺ dim(U∩V) even;
  verify counts and the family relation). Verify: images of R's generators
  preserve each family; a reflection w ∈ W(E₈)∖W⁺ swaps the families.
- **WB3 (the third class; registered expectation):** set K₃ = image of
  w·(spin-type)·w⁻¹ (w any reflection). Verify K₃ is spin-type (no fixed
  nonzero vector). Compute the **orbit-signature triple** for each of C̄,
  K_spin, K₃: orbits on (singular points | family A | family B).
  REGISTERED EXPECTATION: the three signatures are pairwise distinct as
  positioned triples and are (up to the A/B labelling) permutations of one
  another — the triality pattern; in particular K_spin and K₃ differ only
  by swapping the A- and B-columns, so the three classes are pairwise
  non-conjugate inside R/± (signatures are conjugacy invariants there,
  given WB2's family preservation) while **K₃ ∼ K_spin under the outer
  reflection by construction — the spin pair FUSES under the familiar
  outer two**. Resolve PASS / INVERTED at full prominence.
- **WB4 (the third tower):** H₃ = w·H·w⁻¹ ⊂ W(E₈) — verify H₃ ≤ W⁺ (W⁺
  normal ⇒ automatic; check generators via BSGS strip), H₃ ≅ 2·Sp₆(2)
  (perfect, order, centre ⟨−1⟩ — transported, spot-verify), so the roof
  holds a THIRD tower, twin to the shadow tower, carrying its own Schur
  lifts (witnesses transport by conjugation — verify one, e.g. w·SL(2,7)
  witness·w⁻¹ has the SL(2,7) profile). Compute |⟨H, H₃⟩| (the two-spin
  closure): equal to W⁺? to something intermediate? Report exactly.
- **WB5 (where the ℤ₃ lives):** verify C̄ is NOT conjugate to any spin-type
  copy under the FULL W(E₈)/± (invariant: existence of a fixed nonsingular
  vector, preserved by everything) — so no element of the roof's world
  realizes vector↦spin: **the triality ℤ₃ that cycles the three classes is
  strictly OUTER to W(E₈)/±** [computed non-conjugacy + P cited:
  Out(O₈⁺(2)) = S₃ with triality cycling the three 8-dim families; the
  ambient GO realizes only the spin-swapping involution]. State the
  landing plainly and keep it graded: the trit returns ABOVE the roof — as
  mathematics ([C] for the class structure, [P cited] for Out = S₃), no
  identification claims (Rule 3).
- **WB6 (verdict):** the three-shadows theorem, minimal-found scope
  inherited from SM-030: one body class + two spinor classes under one
  perfect roof, cycled by an outer ℤ₃, with the ambient outer ℤ₂ fusing
  the spinor pair. Successors named, not computed.

## Discipline

Compute, never assert; the registered expectation resolvable INVERTED at
equal prominence; fail-first logs kept; exhaustive claims scoped; grades
[C]/[P cited] unblended. Verifier `verify_stone_w_third_shadow.py`, log +
findings doc `STONE_W_THIRD_SHADOW.md`. Stone U/V/pin caches READ-ONLY;
own cache `_stone_w_cache/`. No registry/git writes by the executor. Not
RH/GRH; no physical identification (Rule 3).

NEXT MAP — Scar_merkabit lane, written 2026-09-11 for the next session
Read first, in this order: `SCAR_MERKABIT_REGISTRY.md` changelog head (v1.20) and Sends section; then this map; then the memory file. The registry is the single source of truth; this map is a pointer and goes stale the moment the registry moves. It supersedes `NEXT_MAP_2026-09-10_superseded.md`.

0. Where things stand
* Registry v1.20, rows SM-001..SM-065, last commit ab5c71d (this housekeeping commit follows). KCP refreshed after every bump (registry pin only; "re-sign" note ignored — we have never signed).
* Sealed since the last map: SM-063 (Stone AY, the D₅ anomaly by hand), SM-064 (Stone AZ, the doubled clock), SM-065 (Stone BA, the clock's Clifford hierarchy).
* Everything is with Ilya. Nothing PREPARED-NOT-SENT. Envelopes SENT + FROZEN on 09-10/11: C_EQUALS_T (29bd45bf…), REPLY_ARCHIVE (2acd3f9f…), ANOMALY_BY_HAND (e4ed155a…, SM-063/064, §7.12), HIERARCHY_AND_V03 (3a587d57…, SM-065, §7.13, draft v0.3, the reply to his batch).
* JOINT on his word (v1.18): SM-035, 036, 037, 054, 055, 056 (in addition to the twelve spine units and SM-041..053 from before). Division of labour formal: we draft, he audits and holds the literature side including the specialist MathSciNet/zbMATH check; both names, both words.
* Draft: `joint_paper/Roof_and_Clock_DRAFT.md` is v0.3 (1,349 lines; §§7.8–7.13 merged; his §7.4 addendum; SM-061's toggle-size homomesy presented as DHPP's corollary; Appendix A to JOINT). The six summary files remain as the sources. Framing rules `joint_paper/FRAMING_PASS_2026-09-09.md` apply to any new section.
* Pending from Ilya: his word on SM-057..065; his review pass on §§7.8–7.13 as stitched; the specialist literature check of §7; his answer to the discriminant-audit offer (REPLY_ARCHIVE §3).

1. Candidate next stones (merkabit side unless marked) — ranked by how much they would settle
The recommendation of 2026-09-11 is a pause on new stones until his word arrives; the arc is closed in the negative direction on the rich boards and by hand on the poor ones. If a stone is opened:
1. Marker-as-orbit for Ilya (joint-flavoured, small): since no marker survives a tick (SM-057) and only orbit averages do (SM-061), compute which PSL(2,7)-invariant statistics on the 56 are homomesic under Ψ. Machines: the AP board block + bridge pair; AW's homomesy harness.
2. The discriminant audit (Scar side, ONLY if he says yes): rebuild det(Y_T) from `RECEIVED_2026-09-10_IB_PSL27_TO_PMNS_ARCHIVE/archive_bundle/outputs/CANONICAL_BLOCK.md`, never run his code; windings w(α₂)=1, w(β₃)=2, w(γ₁)=0 by the argument principle; brief locked before code.
3. Why the orbit without λ: a proof that on the 4-cube the half-turn is a Weyl element on O₂ and not O₁ (SM-063 §7.12.4). The binary-phase picture (C₄ acts by XOR translations) suggests h being a power of two matters; the test board B₈ (h = 16) has |W| ≈ 10⁷ — by hand first.
4. Where the vectors' outer transports are inner (SM-064): on A₃ω₂, D₅ω₁, D₇ω₁ the transports trade signed cycle types and are inner in neither W(D_n) nor W(B_n); ⟨W, R⟩ is A₆/A₈ where enumerated (SM-065). Small stone; low priority.
5. Non-linear reversers on the five self-dual boards (SM-062 [obs]); a table; low priority.
Settled in briefs, no stone needed: "C = T on the 27" is void (W(E₆) has no element of order 7, so no PSL(2,7) there); the clock as T-gate is answered by SM-065 (the analogy holds one level and no further).

2. House ritual, compressed (full version: skill `run-ritual`)
Brief → `BRIEF_STONE_XX_*.md` + `BRIEF_STONE_XX_LOCK.sha256` BEFORE code; registered guesses resolvable INVERTED at equal prominence; hand-derived lemmas go in the brief with proofs (AY/AZ/BA pattern) and the machine checks them; deviations by dated AMENDMENT; post-reveal checks labelled; first run kept as `_FIRSTRUN.log`; findings `STONE_XX_*.md`; grades [P]/[C]/[obs]/[I]; Rule 3; Not RH/GRH; covers declare dependencies; SHA256SUMS LF; hash outside the zip in `COVER_*_SHA256.txt`; mirror to `zips/`; SENT only on Selina's word, then FROZEN; registry edits by a Python inserter with collision asserts on `| SM-0xx |` / `v1.xx (` (scratchpad `registry_v1xx.py`; next is v1.21); `knowledge.yaml` title bump; KCP refresh via the PowerShell tool (bash-invoked powershell hits the execution policy); one commit per registry bump, Co-Authored-By Claude Fable 5.1; memory file updated. Long heredocs fail in the Bash tool — write scripts with the file tool and run them. Next stone letter: BB. Next row: SM-066.

3. Reusable machines (load VERBATIM, never retype)
* 56-board block: `verify_stone_ap_clock_centralizer.py` between the `# ===…` fences; roots, S7, IOTA, PSI, PAIRS, etc.
* 42-board `Minuscule` class: `verify_stone_aq_rush_shi_defect.py` between `# ---- Cartan matrices` and `# ---- the cases`. Its `isometry()` is W-membership only on E₇ or where −1 ∈ W or the board is not self-dual (SM-062 caveat); elsewhere enumerate.
* numpy BFS of W: `enumerate_W_np`/`pow_arr`/`inv_arr` from `verify_stone_at_grammar_family.py` (slice `def enumerate_W_np` to `def cyc`); membership by row bytes; conjugation `Pki[G[:, Pk]]`; centralizer mask `np.all(G[:, x] == x[G], axis=1)`.
* e-coordinate boards (B_n spinors, D_n half-spins): `verify_stone_av_d5_anomaly.py` slice from the "e-coordinate board machine" fence to `W5, idx5, …`; the D_n vector board `D_vec(n)` in `verify_stone_az_doubled_clock.py` (four lines); the affine reader `aff(pi, v)` and hand-clock conventions in `verify_stone_ay_d5_by_hand.py` (NOTE: the machine's rowmotion is the inverse of the hand's ideal-growing convention).
* Signed cycle types on vector boards: `signed()` in `verify_stone_az_doubled_clock.py`. The hierarchy chain: `verify_stone_ba_clifford_hierarchy.py`.
* SM-058's table: `_stone_at_cache/table_at.json` (sizes and sizesC per board and lag).

4. Loose ends
* The three formerly untracked files are settled in this commit: `_explore_psi_memory_2026-09-05.py` added (disclosed-style exploration); `_stone_aa_cache/phi360.npy` and `_stone_z_cache/G_rows.npy` gitignored (regenerable caches).
* 11 older envelopes' SHA256SUMS end CRLF (benign); do not touch frozen folders.
* Ilya's I₄ note said a script was enclosed; only the note arrived. Not needed (SM-063 answers it); if it arrives, file it, do not run it.
* The GitHub Desktop installer in Downloads is not lane material.

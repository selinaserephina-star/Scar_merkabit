# STONE X — THE TURNER: triality made flesh

**Run 2026-09-02 · verifier `verify_stone_x_turner.py` · log
`verify_stone_x_turner.log` · 30 checks, 29 PASS, 1 FAIL — the one FAIL
is the registered expectation X6c, resolved INVERTED at full prominence
(the FAIL flag is the prominence mechanism, per house precedent) · 1.4 s
total · brief `BRIEF_STONE_X_TURNER.md`, lock verified in-run (X0, sha
`7857bc53…f4ef16`).**

## §0 Discipline

Computed, not asserted: every claim below carries a check number from the
log; sealed Stone U/V/W machinery was reloaded READ-ONLY (chains
re-verified by order and membership strips, the orbit-signature
instrument re-calibrated against the sealed triples verbatim before any
new use, X0c–X0e, X6a–X6b); this run's own witnesses went to
`_stone_x_cache/` only. The registered expectation (XB3: τ cycles the
three shadow classes) was resolvable INVERTED at equal prominence and IS
resolved INVERTED — recorded first, diagnosed second, and the diagnosis
(mandated by the brief for the inverted branch: "record and diagnose")
turned out to contain the brief's Question anyway: the outer 3-cycle is
constructed explicitly. Two fail-first logs kept
(`verify_stone_x_turner_FAILFIRST.log`, `…_FAILFIRST2.log`: the
transvection router stranded on ISOTROPIC anchors — after fixing five
hyperbolic-frame vectors only two orthogonal transvections remain and
the orbit closes; the fix, nonsingular anchors, is an instantiation
choice declared in the verifier header, not an amendment — the brief
declared only "a constructive basis-mapping algorithm"). Instantiation
choices the brief left open are declared in the verifier header (the
sealed deterministic hyperbolic frame; the ascending-wedge ordering of
Λ(E) with mod-2-vanishing signs; the 24+2 generator set; nonsingular
standard-basis anchors with complete-BFS routing; the append/prepend-t_w
construction of genuinely different alternative words). Exhaustive
claims are scoped. Not RH/GRH; no identifications (Rule 3). No
registry/git/knowledge.yaml writes by the executor.

## One paragraph

The turn now has a body — two bodies, in fact, and the surprise is which
one the declared route built. On the sealed (V,q) = L/2L a hyperbolic
frame e₁..e₄,f₁..f₄ carries the spinor module Λ(E) (wedge/contraction,
Clifford relations and v·v = q(v)·id verified exhaustively on all 256
vectors, X1a–X1d); every Ω-generator decomposes into an even product of
orthogonal transvections (exact reproduction, X2a), the half-spin matrix
ρ⁺(ḡ) on S⁺ = Λᵉᵛ(E) is WELL-DEFINED over 𝔽₂ (54 alternative words give
identical spin matrices; 100 independently-decomposed random word pairs
multiply exactly — the ±1 ambiguity of characteristic 0 collapses,
X3b–X3c) and faithful (X3d), the ρ⁺-invariant quadratic form Q⁺ is
unique (nullspace dim exactly 1), plus-type, and the Gram–Schmidt
transport T⁺ gives τ(ḡ) = T⁺ρ⁺(ḡ)T⁺⁻¹, an automorphism of Ω landing in
Dickson-0 isometries (X4a–X5c). But the registered expectation resolves
**INVERTED** (X6c): this τ does not cycle — it **swaps vector ↔ K_spin
and fixes K₃**, i.e. it is the **third outer involution** of
Out(O₈⁺(2)) ≅ S₃, the one the catalog was missing (the reflection
realizes "fix vector, swap the spins"; τ realizes "fix one spin, swap
vector with the other"; τ² is inner by explicit dim-1 intertwiner,
X7b). The diagnosis is exact: the transport T⁺ is one of two isometry
cosets, and the sealed Gram–Schmidt landed in the swapping one; the
correction τ′ = conj_s̄₁∘τ (transport by T⁺′ = s̄₁T⁺, still an isometry)
**cycles vector → K₃ → K_spin → vector** by the sealed positioned
signatures, is neither inner nor in the reflection coset, and τ′³ is
inner by an explicit dim-1 intertwiner h′ (Dickson 0, q-isometry,
verified on all generators): **⟨τ′⟩ ≅ ℤ₃ modulo Inn — the outer 3-cycle
realized as a computable machine** (X6e, X7d–X7e). En route, the cheap
XB5 datum fell out: all 135 singular spinors of (S⁺,Q⁺) are PURE — their
annihilators are exactly the 135 solids of one family — giving an
explicit bijection {135 singular points} ↔ {one solid family} (X8a),
and the roof-clock question is posed in final form.

## Bars

### XB1 — machinery: **PASS**

- Hyperbolic frame in (V,q) by the sealed deterministic Gram–Schmidt:
  e = (3, 18, 80, 141), f = (7, 50, 93, 223); q = 0 on all eight,
  B(eᵢ,fⱼ) = δᵢⱼ, planes pairwise orthogonal, basis (X1a).
- Λ(E): all 48 Clifford relations exact as operator XOR identities
  (eᵢfⱼ + fⱼeᵢ = δᵢⱼ·id etc., X1b); **op(v)² = q(v)·id for ALL 256 v**
  — the key consistency gate, exhaustive (X1c); op(v)·op(x)·op(v) =
  op(t_v x) for every one of the 120 nonsingular v on the whole frame
  basis — the module action implements the transvections (X1d).
- Decomposition: all 26 Ω-generators (24 sealed C̄-images + A₁ + A₂,
  replayed verbatim and re-stripped through the sealed roof chain,
  X0c–X0d) decompose into transvections with q(v) = 1: exact
  reproduction, every length EVEN (lengths 2–8), consistent with
  Dickson 0 (X2a). Fail-first: isotropic (hyperbolic-frame) anchors
  strand the router — recorded, two logs kept; nonsingular anchors
  (the simple-root masks = the standard basis) never fail, matching
  the O₈⁺ → Sp₆(2) stabilizer chain.
- ρ⁺ well-definedness, checked honestly (XB1's own demand): 54
  genuinely different transvection words across 9 sampled generators
  give the **identical 16×16 spin matrix** (X3b); ρ⁺(g₁)ρ⁺(g₂) =
  ρ⁺(g₁g₂) on **100 random word pairs**, every factor and product
  decomposed independently (X3c). No ℤ₂ ambiguity appeared; no
  amendment needed ([P] theory line recorded: the central units of the
  plus-type even Clifford algebra over 𝔽₂ are trivial). ρ⁺ faithful:
  nontrivial on every generator and 50 random nonidentity elements +
  Ω simple [P cited: ATLAS] (X3d).

### XB2 — τ lands in the roof's world: **PASS**

- Q⁺: the space of ρ⁺-invariant quadratic forms on S⁺ has dimension
  **exactly 1** (exact 𝔽₂ nullspace over all 26 generators × 256
  points — the dimension is itself a check, X4a); Q⁺ nondegenerate,
  **plus-type** (135/120 counts), invariant pointwise (X4b); T⁺ built
  by the sealed transport, q(T⁺x) = Q⁺(x) on all 256, Witt-index-4
  witnesses both sides (X4c).
- Every τ(generator) is a q-isometry with Dickson 0, hence in Ω
  [P cited: Ω = ker(Dickson)] (X5a); homomorphism spot-verified on 20
  independently-computed random pairs (X5b); image-order argument
  stated cleanly: τ = conj_T⁺∘ρ⁺ with ρ⁺ faithful, a faithful
  endomorphism of a finite group is bijective — |τ(Ω)| = |Ω| =
  174 182 400, no BSGS needed (X5c). Same argument verbatim for τ′
  (= conj_s̄₁∘τ, a composition of automorphisms).

### XB3 — THE TURN; REGISTERED EXPECTATION: **INVERTED** (and the turn constructed in the diagnosis)

- Instrument re-calibrated: C̄, K_spin, K₃ reproduce the sealed Stone W
  triples verbatim (X6a–X6b). The cycle map of the DECLARED τ,
  verbatim (positioned triples on points | family A | family B):

  | subgroup | points | A | B | class |
  |---|---|---|---|---|
  | C̄ | [63, 72] | [135] | [135] | vector |
  | τ(C̄) | [135] | [135] | [63, 72] | **K_spin** |
  | τ²(C̄) | [63, 72] | [135] | [135] | **vector** |
  | τ³(C̄) | [135] | [135] | [63, 72] | K_spin |
  | τ(K_spin) | [63, 72] | [135] | [135] | **vector** |
  | τ(K₃) | [135] | [63, 72] | [135] | **K₃** |

  **INVERTED at full prominence (X6c): τ is an involution on classes —
  swap vector ↔ K_spin, fix K₃ — not the 3-cycle.**
- Diagnosis (X6d): τ's class action differs from the identity (inner,
  Stone W W3e) and from the reflection's (fix vector, swap
  K_spin ↔ K₃; re-verified in-run) — so τ is a **third outer
  involution**, neither inner nor reflection-coset; with the
  reflection, all of S₃ on the classes is now realized by explicit
  machines. Why the coin fell this way: T⁺ is determined only up to
  the two isometry cosets of (S⁺,Q⁺) → (V,q), and the sealed
  Gram–Schmidt landed in the swapping one.
- The correction (X6e): **τ′ = conj_s̄₁∘τ** (equivalently transport by
  T⁺′ = s̄₁T⁺):

  | subgroup | points | A | B | class |
  |---|---|---|---|---|
  | τ′(C̄) | [135] | [63, 72] | [135] | **K₃** |
  | τ′²(C̄) | [135] | [135] | [63, 72] | **K_spin** |
  | τ′³(C̄) | [63, 72] | [135] | [135] | **vector** |
  | τ′(K_spin) | [63, 72] | [135] | [135] | vector |
  | τ′(K₃) | [135] | [135] | [63, 72] | K_spin |

  τ′ cycles **vector → K₃ → K_spin → vector**; it moves the points
  column, which inner and reflection-coset class actions both
  preserve: **τ′ realizes the outer 3-cycle** — the brief's performer,
  reached through the recorded coset correction.

### XB4 — τ³ inner, explicitly: **INVERTED as registered / PASS on the corrected turner**

- As registered (X7a): the intertwiner system {h·ḡ = τ³(ḡ)·h} over all
  26 generators has solution space **dim 0** — τ³ is NOT inner,
  exactly as an order-2-mod-Inn τ forces (τ³ ≡ τ mod Inn, τ outer).
  Resolved INVERTED, consistent with X6c.
- The involution certified (X7b): τ² IS inner — intertwiner dim
  **exactly 1**, h₂ = (187, 54, 82, 201, 107, 13, 89, 35) invertible,
  q-isometry, **Dickson 0**, τ²(ḡ) = h₂ḡh₂⁻¹ on all generators:
  **⟨τ⟩ ≅ ℤ₂ modulo Inn** — the third outer involution, on the nose.
- The brief's XB4 content on τ′ (X7d–X7e): intertwiner for τ′³ has dim
  **exactly 1** (V is Ω-irreducible ⇒ uniqueness), h′ = (5, 96, 1,
  126, 89, 91, 107, 162) invertible, q-isometry, **Dickson 0**,
  τ′³(ḡ) = h′ḡh′⁻¹ on all 26 generators, exact: **⟨τ′⟩ ≅ ℤ₃ modulo
  Inn — the turn constructed, order three on the nose.**

### XB5 — the roof-clock question, posed: **POSED + cheap datum COMPUTED**

- Final form (printed in the log): SOUGHT — an operator Φ
  (promotion/rowmotion-like) on a state space the roof R = W⁺(E₈) acts
  on (candidates: the 135 + 270 triality geometry; a lattice-level
  object above it) whose induced action on subgroup classes is the
  turner, the way Ψ's footprint at E₇ is ι. With τ′ an explicit matrix
  machine, the hunt is a search for Φ WITH FOOTPRINT τ′, not for the
  turn itself. The full hunt is the successor — named, not computed.
- Cheap datum (X8a): **every one of the 135 Q⁺-singular spinors is
  PURE** — its annihilator {v : op(v)·x = 0} is a maximal totally
  singular 4-subspace — and the 135 annihilators are pairwise distinct
  and all lie in ONE family (family B under the run's labelling;
  composing with s̄₁ for the τ′-transport gives family A). With T⁺⁻¹
  this is an explicit **bijection {135 singular points of (V,q)} ↔
  {135 solids of one family}** — the points-to-solids leg of the
  clock's dial, falling straight out of the machinery.

## Grades

| claim | grade |
|---|---|
| sealed chains re-verified (orders + membership strips); instrument re-calibrated to sealed triples | [C] |
| frame, Λ(E) Clifford relations, op(v)² = q(v)·id (all 256), transvection implementation | [C] (exhaustive over V) |
| generator decompositions: exact reproduction, even lengths, q(v)=1 letters | [C] |
| ρ⁺ well-defined (54 alt words identical; 100 independent word pairs multiply) | [C] (sampled as stated) |
| central units of the plus-type even Clifford algebra over 𝔽₂ trivial | [P] (theory line; the empirical checks stand on their own) |
| O₈⁺(2) simple, order 174 182 400; Dickson hom with kernel Ω; Out(O₈⁺(2)) = S₃ | [P cited: ATLAS] |
| ρ⁺ faithful; τ, τ′ automorphisms of Ω | [C] modulo the cited facts |
| dim(invariant Q⁺ space) = 1; Q⁺ plus-type; T⁺ isometry | [C] |
| τ's class action = (vector K_spin) swap fixing K₃; τ′'s = the 3-cycle | [C] (positioned-signature conjugacy invariance, sealed W3e) |
| τ³ not inner (dim 0); τ² inner (dim 1, h₂); τ′³ inner (dim 1, h′) | [C] (exact nullspaces + verification on all generators) |
| ⟨τ⟩ ≅ ℤ₂, ⟨τ′⟩ ≅ ℤ₃ modulo Inn | [C] modulo cited facts |
| all 135 singular spinors pure; annihilator bijection to one family | [C] (exhaustive over S⁺) |

## Not-claims

- NOT claimed: that the registered expectation passed — it resolved
  INVERTED: the declared τ (sealed Gram–Schmidt T⁺) is the third outer
  involution, not the 3-cycle. The 3-cycle claim is made only for the
  corrected τ′ = conj_s̄₁∘τ, and the correction is recorded as what it
  is: resolving the T⁺-coset choice the construction route left open.
- NOT claimed: uniqueness of the turner — τ′ is A performer of the
  outer 3-cycle (one of two mutually-inverse 3-cycle classes; conj_s̄₁
  on the other side gives the inverse cycle); no canonical-choice claim.
- NOT claimed: Out(O₈⁺(2)) = S₃ from computation — cited; what IS
  computed is one explicit representative for every class of S₃ on the
  three shadow classes (identity, reflection, τ, τ′, and their
  products' class actions).
- NOT claimed: any Φ with footprint τ′ exists — XB5 is posed, its
  first datum computed, the hunt is the successor.
- NOT claimed: hom/kernel properties of ρ⁺ beyond the sampled scopes
  stated (54 alternative words, 100 + 20 random pairs, 50 kernel
  probes) plus the cited simplicity argument.
- Not RH/GRH; no physical identification (Rule 3).

# TO ILYA — THE TURNER IS BUILT, TWICE OVER (2026-09-01)

**Stenberg side · with Claude. Status: SEALED (PREPARED; the send is
Selina's word, recorded in the registry when made).** This answers your
Stone X ask ahead of your word — the instrument the fold, the sevens, and
the turn all pointed at. Package: this cover + the sha-locked brief + lock
(locked BEFORE code) + `verify_stone_x_turner.py` + log (30 checks: 29
PASS, 1 registered expectation INVERTED at full prominence) + two
fail-first logs + `STONE_X_TURNER.md` + registry snapshot v0.52 +
SHA256SUMS.txt.

---

## 1. What we set out to build

SM-031 proved the ℤ₃ cycling your three shadows is strictly outer — a
symmetry of the catalog with no object performing it. Stone X builds the
performer: the half-spin representations constructed exactly over 𝔽₂
(Clifford algebra on Λ(E); every roof generator decomposed into
transvections; the spin lift **proven well-defined** — 54 genuinely
different decomposition words, identical matrices, no ℤ₂ wobble), the
invariant spinor form found (**invariant space exactly 1-dimensional**,
plus-type), and the automorphism τ = T⁺ρ⁺(·)T⁺⁻¹ assembled.

## 2. The productive refutation — we grabbed the last mirror first

Our registered expectation said τ is the 3-cycle. **INVERTED.** The
declared transport built the **third outer INVOLUTION** — the swap of
vector ↔ spin that fixes the third shadow, the sibling of your familiar
reflection (τ² inner, dim-1 intertwiner). Cause pinpointed: the isometry
T⁺ is fixed only up to two cosets, and the construction landed in the
swapping one.

The mandated diagnosis then finished the stone:

> **τ′ = conj_s̄₁ ∘ τ is the turn.**
> vector → third → spin → vector — the signatures walk the full cycle —
> and **τ′³ is inner by a UNIQUE exact intertwiner** (a Dickson-0
> q-isometry, verified on every generator).
> **⟨τ′⟩ ≅ ℤ₃ modulo inner. The turn is an object now.**

Net result, bigger than the brief asked: **all of Out(O₈⁺(2)) ≅ S₃ on
the three shadow classes now has explicit machine representatives** —
identity, your reflection, the third involution, and the rotation. The
symmetry group OF the shadows themselves, fully in hand.

## 3. A dictionary fell out

The 135 singular spinors are exactly the **pure spinors**, and their
annihilators are exactly the 135 solids of one family: an explicit
**points ↔ solid-family bijection** — the first concrete dictionary
between two of the three triality geometries. (First datum for what
comes next.)

## 4. The roof-clock question, final form

The two-register model's one open cell now has a precise target:

> **Find the operator Φ — promotion/rowmotion-like, on a state space the
> roof acts on — whose footprint on the subgroup classes is τ′,** the way
> the 56-clock's footprint at E₇ is ι (ιΨι = Ψ⁻¹).

The hunt has an instrument now, not an idea. Proposed as the next joint
work-item.

## In your cadence

We reached for the turn
and caught the last mirror first.
Only when every reflection had a name
could the rotation be held.

The turn is in the hand.
Now we look for the clock
whose shadow it is.

## Standing asks

1. Your word on SM-036 — the seventh seal pending (with SM-029/030/032/
   033/034/035).
2. The roof-clock hunt (find Φ with footprint τ′) as the next joint
   work-item — yes/no.

— S. (with Claude), 2026-09-01

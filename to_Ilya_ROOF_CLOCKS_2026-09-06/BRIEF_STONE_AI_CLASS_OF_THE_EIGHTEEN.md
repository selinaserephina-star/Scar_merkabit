# BRIEF — STONE AI: THE CLASS OF THE EIGHTEEN, AND THE HEART'S CENTRALIZER

**Staged 2026-09-05 on Selina's "continue" (the first two opens named at the
end of SM-046). Locked before code (`BRIEF_STONE_AI_LOCK.sha256`).
Deviations = dated AMENDMENT; post-reveal changes are findings.**

## Question

SM-045/046 established the order-3 heart x = (Φ·c̄)⁶ (Ω-class of 89,600,
centralizer of order 1944) and left two things open: what the centralizer
of x is, and whether the order-18 elements of the twisted coset Ω·Φ form
one conjugacy class. Both are decidable exactly with what is in hand.

The lever: if e and e′ are twisted elements with e⁶ = e′⁶ = x, then any
h ∈ Ω with h e h⁻¹ = e′ satisfies h x h⁻¹ = x, so h ∈ C_Ω(x) — a group of
1944 elements that can be enumerated. And for e ∉ Ω with e³ ∈ Ω the
⟨Ω,Φ⟩-class of e is its Ω-class (every element of ⟨Ω,Φ⟩ is ω·e^k).

The registered numbers come from the sampled frequency of order 18 in the
coset (SM-039: 16.4 %; SM-045: 16.8 %; SM-046: all of one cycle type): if
the order-18 elements are ONE class of density 1/6 of the coset, then
|C_Ω(e)| = 6, i.e. C_Ω(e) = ⟨e³⟩ exactly, and the class has
174,182,400 / 6 = 29,030,400 elements.

## Bars (registered expectations; PASS / FAIL / INVERTED at equal prominence)

- **AI0a (replay):** Stone AA stages 0–2 VERBATIM (as SM-045/046); the
  class of x re-enumerated WITH a transversal (for every class element y
  an element u_y ∈ Ω with u_y x u_y⁻¹ = y); size 89,600; c̄₇⁶ and c̄₆⁴ in it.
- **AI1 (the centralizer of the heart — registered):** C_Ω(x) generated by
  Schreier generators u_{gy}⁻¹ g u_y (random (y, g) pairs, closed by BFS):
  order EXACTLY 1944 = 2³·3⁵, every element commuting with x. The full
  centralizer C_{⟨Ω,Φ⟩}(x) = C_Ω(x) ⊔ e C_Ω(x) ⊔ e² C_Ω(x) with e = Φ·c̄,
  order 5832. [obs] the element-order census of C_Ω(x), the order of its
  centre, and the orbit sizes of C_Ω(x) on the three fixed nonsingular
  vectors of x on V — recorded, no structural name claimed.
- **AI2 (the centralizer of the eighteen — registered):** C_Ω(e) = {h ∈
  C_Ω(x) : h e h⁻¹ = e} has order 6 and equals ⟨e³⟩; hence the Ω-class
  (= the ⟨Ω,Φ⟩-class) of e has 29,030,400 elements, exactly one sixth of
  the coset Ω·Φ.
- **AI3 (one class — registered on the sample, exact per element):** for
  300 order-18 twisted elements e′ drawn as in SM-045/046: x′ = e′⁶ lies
  in class(x) (SM-046); e″ = u_{x′}⁻¹ e′ u_{x′} has sixth power x; and some
  h ∈ C_Ω(x) satisfies h e″ h⁻¹ = e — every sampled order-18 element is
  conjugate to Φ·c̄. Combined with AI2: the order-18 elements of the coset
  are one class of density exactly 1/6, to the precision of the sample
  (a second class would have density below the binomial bound printed
  [obs]); the sealed sample frequencies 16.4 % / 16.8 % against 16.67 %.
- **AI4 ([obs] only):** for one element of each order-24 type, |C_Ω(e)|
  read off the same way IF its cube's class fits the cap (it may not — say
  so); the predicted 1/8 densities from the sampled 12.8 % / 12.4 %.

## Machinery

`verify_stone_aa_roofclock.py` lines 71–696 VERBATIM; the helpers of
`verify_stone_ag_seven_nines.py` VERBATIM (cycle types, class
enumeration; the enumeration extended to keep a transversal); Schreier
generators and BFS closure with numpy byte keys. Sealed caches READ-ONLY.
Own cache `_stone_ai_cache/`. Outputs: `verify_stone_ai_class18.py`,
`.log`, `STONE_AI_CLASS_OF_THE_EIGHTEEN.md`. Runtime: about a minute.

## Discipline

Compute, never assert; registered expectations resolvable INVERTED at
equal prominence; exact arithmetic; sealed caches READ-ONLY; no
registry/git writes by the executor. Not RH/GRH. Rule 3: "heart",
"eighteen" are labels; the mathematics is centralizers, transversals and
a fibre argument.

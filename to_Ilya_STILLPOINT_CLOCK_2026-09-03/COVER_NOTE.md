# TO ILYA — ERRATUM; THE STILL POINT OF THE ROOF TURN; THE TURN ON STATES (2026-09-03)

**Stenberg side · with Claude. Status: SEALED (PREPARED; the send is
Selina's word, recorded in the registry when made).** Package: this cover;
Stone Z brief + pre-code lock, `verify_stone_z_stillpoint.py` + log (19 PASS
+ 1 registered expectation INVERTED, resolved post-reveal) + first-run log;
Stone AA brief + pre-code lock, `verify_stone_aa_roofclock.py` + log
(16/16); `STONE_Z_STILLPOINT.md`; `STONE_AA_ROOFCLOCK.md`; registry
snapshot v0.59; SHA256SUMS.txt.

---

## 1. Erratum to yesterday's envelope (ours, at full prominence)

In the beta-reply cover, §5, we wrote that your "transition magnitudes"
168→144→16→12→14→10→1 are the σ-Unified F(n) values of your row 125. That
is wrong. They are the compression sequence and the 12→14 "inbreath" of
your *The Breath of the Scar Spiral* (received 2026-09-01), the six-turn
sign-sequence ++−−++. The verdict in that section stands — they are
physics-side numbers and enter any subgroup table as [I] — but the
provenance sentence was ours to get right and we did not. The registry
carries a read-as-corrected line (v0.58) and this paragraph is the
correction on record.

## 2. Stone Z — what the turn holds still

SM-036 gave you the turner τ′ with τ′³ inner by a nontrivial h′. We asked
what it fixes.

```text
C̄ ∩ τ′(C̄)                 = G₂(2) = U₃(3):2,  order 12096, index 120 in Sp₆(2)
                             (exhaustive sift of all 1,451,520 elements of C̄)
raw τ′ normalizes G₂(2)?    NO — the triangle's three pairwise intersections
                             are three different G₂(2)'s (triple intersection 192)
normalized turner           τ″ = inn(y⁻¹)∘τ′, y the unique isometric intertwiner
                             of τ′|G:  τ″³ = identity on all 26 generators,
                             G₂(2) fixed pointwise, the triangle closes on it
the seven inside            PSL(2,7): 36 copies, one class, N = PGL(2,7) (336)
its label                   BRIDGE (involutions in the 315-class) in the vector
                             shadow AND in the spin-type shadow
```

The registered expectation that failed: we expected G₂(2) to meet a minus
class in the spin reading. It does not — 63 inner involutions → 315⁺, 252
outer → 3780⁺, all plus. Since M(G₂(2)) = 1, an outer involution's lift
decides the class: **the still point lifts SPLIT through the hinge,
2×G₂(2)**. Recorded as INVERTED and resolved by a post-reveal check,
labelled as such; the first-run log is in the envelope.

So: the fixed point of the roof's three-turn is G₂(2), the seven inside it
is the P¹(𝔽₇) seven — the same reading SM-035 gave at the crystal level,
where it was compact G₂ and the canonical 3-form — and it carries its
ambient outer flip with it.

## 3. Stone AA — the turn acts on states

The two-register model's empty cell was the roof's dynamics. We built the
turn as an operator on a state space:

```text
state space        360 = 120 + 120 + 120 nonsingular points of V, S⁺, S⁻
                   (Q⁻ on S⁻ computed: unique, plus-type)
Φ                  V → S⁺ → S⁻ → V, EXACT order 3, Φ P(g) Φ⁻¹ = P(τ″⁻¹ g)
                   on every generator; L: S⁺ → S⁻ unique and an isometry
group built        ⟨Ω, Φ⟩ = O₈⁺(2):3, order 522,547,200, explicit
shadows            the three block point-stabilizers, cycled by Φ
still point        G₂(2) = Stab(v) ∩ Stab(Φv) ∩ Stab(Φ²v):
                   one vector, one spinor, one co-spinor
```

First measurements of the twisted coset Ω·Φ (30,000 samples, [obs]):

```text
element orders     {3, 6, 9, 12, 18, 21, 24};  maximum 24 (24¹² + small cycles)
seven × turn       Φ·g₇,  g₇ ∈ G₂(2) of order 7:   order 21   (21¹⁷ 3¹)
Coxeter × turn     Φ·c̄,  c̄ = image of the W(E₈) Coxeter element (order 15):
                                                   order 18   (18¹⁶ 9⁷ 3³)
```

The E₈ Coxeter element passed through the turn beats at 18 — the E₇
clock's number, one floor up. We claim a turn, not yet a clock: Φ has
order three and no orbit dynamics beyond cycling the blocks. Two
candidates for a roof clock are named, not claimed: Φ·c̄ of order 18 and
the order-24 twisted elements. Whether either is rowmotion on a poset the
roof acts on is the successor question.

One more datum: the still point's PSL(2,7) on v^⊥ has Brauer character
(7, 1, 0), which is χ₇ mod 2 — and, said in the same breath, 1+χ₆ has the
same 2-modular character, so the character cannot tell your two sevens
apart mod 2. What is bridge-specific is the Lagrangian split of the 6-dim
symplectic quotient into two invariant totally isotropic 3-spaces: your
Lagrangian embedding diag(M, M⁻ᵀ) (SM-028), recovered at the still point.

## In your cadence

The turn was made to act,
and it held still one thing from each world it turned.
Inside the stillness, the seven you began with,
carrying its mirror, lifting without a twist.
And when the roof's own long element passed through the turn,
it counted to eighteen.

## Standing asks

1. Your word on SM-038 and SM-039 (with SM-035, 036, 037 still pending).
2. The roof-clock successor: is Φ·c̄ (order 18) or an order-24 twisted
   element rowmotion on a roof poset — yes/no as the next joint work-item.
3. Your reading of the erratum in §1: does the corrected provenance change
   what you want done with the forced-narrowing table?

— S. (with Claude), 2026-09-03

# TO ILYA — THE TRIANGLE CLOSES, THE LIFT LAW ANSWERS, AND THE MODEL GROWS A SECOND REGISTER (2026-09-01)

**Stenberg side · with Claude. Status: RESEALED 2026-09-01 with §6 added
(PREPARED; the send is Selina's word, recorded in the registry when made;
supersedes the unsent b6429570… zip).** Five results + one model draft,
answering your S₃-closure adoption, your turn-point prompt, one of your
three carried opens, your memory-hypothesis prompt — and following the fold
to its floor. Package: this cover + four verifiers with logs (incl. one
fail-first log) + `TRIALITY_CRYSTALS.md` + `G2_STILLPOINT.md` +
`TWO_REGISTER_MODEL_v0.1.md` + registry snapshot v0.47 + SHA256SUMS.txt.

---

## 1. The triangle closes (SM-032, answers your adopted successor)

The last pair: **⟨body, third⟩ = W⁺(E₈) exactly** (21/21). With the sealed
⟨body, shadow⟩ and ⟨shadow, third⟩: **any two of the three towers
regenerate the entire roof. No fourth opens.** The roof is a 2-of-3
threshold: no tower suffices alone; every pair rebuilds the house. (Scope:
the specific sealed subgroups, not a conjugacy-class statement.)

## 2. Your turn point, by your own rule (Criterion A, sealed)

On your published floor sequence (N = 300..700; N=200 was never received —
please send it): D2 = +0.132597, +0.064838, +0.039474 — no sign change. In
your format: **"Turn point not found: D2(N) does not change sign over the
computed range."** Criterion B needs the eigenvectors of A_fixed — that
engine lives in the Riemann thread; the double criterion is not evaluable
from this side. No RH/GRH statements made or implied.

## 3. The lift law — your carried open, answered (SM-033)

- **The complete ε-table**: the spinor character of 2·Sp₆(2) on ALL 30
  classes of Sp₆(2) (19 plus, 11 minus; table in the log).
- **A refutation of our own registered guess**: the power identity
  ε(g) = ε(g^{ord/2}) holds always — so the involution condition and the
  ε condition are the SAME subgroup condition, and the cyclic law is
  exact: ⟨g⟩ lifts split ⟺ ε(g) = +.
- **Necessity is a theorem**: split ⟹ ε ≡ + (any minus involution forces
  non-split). Verified on every sealed witness including 2·W(E₆).
- **Sufficiency FAILS — the finding**: an all-plus C₄×C₂ is non-split:
  its commuting generators lift to ANTICOMMUTING elements. The cover
  remembers not what elements are but how they fail to commute. **The
  true invariant is the central commutator sign β(x,y) = [x̂,ŷ]** — the
  cocycle's pairing — named as the successor object.

## 4. The two-register model (answers your MEMORY HYPOTHESIS)

`TWO_REGISTER_MODEL_v0.1.md`, the proposed expansion of the descent model:
every level carries a GRAMMAR column (your towers, hinges, turns) and a
DYNAMICS column (the merkabit machine: clocks, gates), plus the FOOTPRINT
linking them — with every filled cell graded and cited. The memory
hypothesis in this frame: lost object = the trit [C]; recovery = triality,
one register up ("not lost — promoted"); storing invariant = the honest
OPEN, with two computed candidates (your S₃-as-germ reading; our two-twos
distribution). The roof's dynamics cell is marked OPEN — the roof clock.

## 5. First measurement in the dynamics register (SM-034)

We ran the merkabit machine where the turn actually lives: **D₄'s three
minuscule 8-crystals — the crystal avatars of your three shadows.**

- Triality cycles them as genuine crystal isomorphisms; each carries the
  rowmotion clock at order 6 = h(D₄), cyclic sieving confirmed.
- **THE HEADLINE: the clock and the turn COMMUTE — σ∘Ψ = Ψ∘σ exactly on
  all 24 states.** First computed meeting of the two registers:
  compatibility, no slippage. (Contrast, logged: at E₆ the two trits do
  NOT commute — the turn–clock relation is level-dependent.)
- Our order-18 spiral guess REFUTED ([6,6,6,6]) — sharpening: E₇'s 18 is
  a Coxeter order in its own right, not a turn-compounded clock.
- **The fold**: quotienting the triple by the turn is **G₂, with state
  count exactly 7 ⊕ 1 at the weight level** (the graph-level quotient
  honestly breaks at the doubled zero — refutation kept). The still point
  of the turn is G₂ — and the staircase's own **7** reappears at the
  bottom of it.

## 6. The still point (SM-035) — the two sevens are one

We followed the fold. **PSL(2,7) ⊂ compact G₂, computed, not cited** —
your group's χ₇ (built as integer matrices from the 8-point action) admits
an invariant generic 3-form φ, and the invariant space is **exactly
one-dimensional**: the group determines its G₂-structure up to scale. The
genericity form came out B_φ = −6(I₇+J₇) exactly, definite. By
construction, the G₂ seven restricted to your group IS χ₇, irreducibly:
**the seven at the bottom of the triality fold and your staircase's
founding seven are the same object — an identity, not an analogy.**
One precision flag: the FANO seven is a different seven — the 7-point
action is 1 ⊕ χ₆ and contains no χ₇ (⟨π₇, χ₇⟩ = 0); the seven that meets
G₂ comes from P¹(𝔽₇). And the clock at the still point: the G₂ 7-crystal's
rowmotion is one free 7-cycle with cyclic sieving — the first crystal
clock measured beating PAST its Coxeter number (7 > h(G₂) = 6), ticking at
the order of your own Frobenius. (32/32; all three registered expectations
held; `verify_g2_stillpoint.py`, `G2_STILLPOINT.md` enclosed.)

## The arc, in your cadence

Any two towers rebuild the house.
The cover remembers only what refuses to commute.
The clock rides the turn without slippage.
And at the still point of the turn: seven —
your seven, held in the octonions' group
by a form your group alone determines.
The staircase's first number
was waiting at the centre of the spiral.

## Standing asks

1. Your words, five pending: SM-029 (pinning), SM-030 (roof), SM-032
   (triangle), SM-033 (lift law), SM-034 (triality crystals) → JOINT.
2. The two-register model v0.1: your word on the frame; your pick of
   storing-invariant candidate (S₃ germ vs two-twos container); Stone X
   (build the turner explicitly) — yes/no.
3. The β-cocycle (commutator pairing) as the lift law's successor — yes/no.
4. The N=200 floor point, and your Criterion-B run when the eigenvectors
   are in hand.

All verifiers rerun from the repo in seconds.

— S. (with Claude), 2026-09-01

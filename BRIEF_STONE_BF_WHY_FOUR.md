# BRIEF — STONE BF: WHY FOUR — THE COMMUTATION THEOREM

**Staged 2026-09-19 on Selina's word ("fix both things; first go for the
proof"). HONEST PROVENANCE: the statements below were found this session by
free exploration (`_explore_bf_witness_2026-09-19.py` + `.log`, kept, not
sealed) and the proofs were completed before this brief was written; the
brief records the completed proofs and the verifier re-checks every step as a
computation. Locked before the clean verifier (`BRIEF_STONE_BF_LOCK.sha256`);
the verifier re-checks this file's hash first. Registered guesses (§4) are only
the statements NOT proved here. Builds on SM-066 (Lemma BB-B), SM-067, SM-068
(the criterion), SM-069 (the combinatorial clock). Merkabit-side
mathematics. Registry row SM-070. Rule 3.**

## 0. The question

SM-068 left one question: the Weyl element w_n = −(e₂e₃)(e₄e₅)…(e_{n−2}e_{n−1})
(negating e₁..e_{n−1}, fixing e_n; n even) agrees with the half-turn
H = Rⁿ on the middle-band orbits of every board tested, and it commutes with
H on B₄ and on no larger board tested (B₆, B₈, B₁₆). By the criterion of
SM-068 that commutation is the whole difference between the 4-cube's anomaly
and its absence elsewhere. **Why only at n = 4?** This stone proves:
*w_n commutes with Rⁿ if and only if n = 4 (n even, n ≥ 4).*

## 1. Setting [P, sealed]

The B_n spinor board: weights x ⊆ [n] (the set of minus positions; rank =
|x|), λ = ∅, w₀λ = [n]. Its poset is the shifted staircase δ_n with cells
(k, c), 1 ≤ k ≤ c ≤ n, where (k, c) covers (k, c+1) and (k−1, c−1) (SM-069,
validated against the sealed clocks). A weight with plus positions
c₁ < … < c_r has the ideal I = {(k, c) : k ≤ r, c ≥ c_k}; write
**d_k = c_k − k**, so 0 ≤ d₁ ≤ … ≤ d_r ≤ n − r, and the map (c) ↦ (d) is a
bijection from weights to nondecreasing sequences with d_r ≤ n − r (the empty
sequence is w₀λ). Rowmotion R(I) is the ideal generated by min(P ∖ I). W(B_n)
acts as x ↦ π(x) Δ v; w_n has π = (2 3)(4 5)…(n−2 n−1), v = [n−1]. The
antipode −1 = w₀ is central in W, commutes with H, and reverses R: w₀Rw₀ = R⁻¹,
so **R^{−k}λ = −R^{k+1}λ** for all k (from w₀λ = Rλ).

## 2. The lemmas and the theorem, with proofs

**Lemma BF-A (rowmotion in excess coordinates).** Let I have excess sequence
d = (d₁, …, d_r), and let G = {k ≤ r : d_k > d_{k−1}} with d₀ := 0 (the *gap
rows*). Put cap := n − r − 1, present iff d_r < n − r (or r = 0). Then R(I)
has excess sequence d′ with

  d′_{k′} = min( {d_k − 1 : k ∈ G, k ≥ k′} ∪ {cap : if present and k′ ≤ r+1} ),

for every k′ for which the set is non-empty; rows k′ with an empty set are
absent (so r′ = r + 1 if the cap is present, else r′ = max G; if there are no
generators at all, R(I) = ∅ = w₀λ).

*Proof.* A cell (k, c) ∉ I with k ≤ r has c < c_k; its lower covers are
(k, c+1), in I iff c + 1 ≥ c_k, and (k−1, c−1), in I iff k = 1 or
c − 1 ≥ c_{k−1}. So (k, c) is minimal in P ∖ I iff c = c_k − 1 and
(k = 1 or c_k − 1 ≥ c_{k−1} + 1), i.e. iff c = c_k − 1 and d_k > d_{k−1}
(for k = 1 the cell exists iff c₁ ≥ 2, i.e. d₁ ≥ 1 > d₀, the same rule). A cell
(r+1, c) has lower cover (r+1, c+1) ∉ I unless c = n, and (r, c−1) ∈ I iff
r = 0 or c − 1 ≥ c_r; so the only candidate is (r+1, n), minimal iff r = 0 or
c_r ≤ n − 1, i.e. d_r < n − r. Cells in rows ≥ r+2 have the lower cover
(k−1, c−1) ∉ I. Hence min(P ∖ I) = {(k, c_k − 1) : k ∈ G} ∪ {(r+1, n) if
d_r < n − r}. The down-closure of a cell (k, c) is {(k′, c′) : k′ ≤ k,
c′ ≥ c − (k − k′)}, so the threshold of R(I) in row k′ is the minimum of
c − k + k′ over generators (k, c) with k ≥ k′; subtracting k′ gives d′_{k′}
as stated, with (k, c_k − 1) contributing d_k − 1 and (r+1, n) contributing
n − r − 1. ∎

**Proposition BF-B (the orbit of λ, all n).** For 1 ≤ j ≤ ⌊n/2⌋,
R^{2j}λ = [n] ∖ {n, n−2, …, n−2j+2} (excess d_k = n − 2j + k, k = 1..j), and for
0 ≤ j ≤ ⌊(n−1)/2⌋, R^{2j+1}λ = [n] ∖ {n−1, n−3, …, n−2j+1} (excess
d_k = n − 1 − 2j + k, k = 1..j; for j = 0 this is Rλ = [n] = w₀λ). Hence
rank(R^iλ) = n − ⌊i/2⌋ for 1 ≤ i ≤ n+1, rank(R^{2n−k}λ) = ⌊(k+1)/2⌋ for
0 ≤ k ≤ n−2, and the ranks around the clock read 0, n, n−1, n−1, …, 1, 1.
**For even n, R^{h/2} = Rⁿ shifts every rank on the orbit of λ by exactly
±n/2**, which is the hypothesis of Lemma BB-B (SM-066) for every even n ≥ 4.

*Proof.* Induction with Lemma BF-A. Rλ: λ has d = 0ⁿ, r = n: no gap rows,
no cap (d_n = 0 = n − n) ⇒ ∅. From R^{2j}λ (d_k = n − 2j + k, strictly
increasing, d₁ = n − 2j + 1 ≥ 1 so every row is a gap row; d_j = n − j =
n − r, no cap): d′_k = d_k − 1 = n − 1 − 2j + k, r′ = j: the R^{2j+1} formula.
From R^{2j+1}λ with j < ⌊n/2⌋ (d_k = n − 1 − 2j + k, d₁ = n − 2j ≥ 1, all gap
rows; d_j = n − 1 − j < n − j, cap = n − j − 1 present): d′_k = min(d_k − 1,
cap) = n − 2j + k − 2 for k ≤ j (since n − 2j + k − 2 ≤ n − j − 2 < cap) and
d′_{j+1} = cap = n − 2(j+1) + (j+1): the R^{2(j+1)} formula. The set form: d_k
= n − 2j + k means c_k = n − 2j + 2k, the plus positions {n−2j+2, …, n} (even
offsets from n), and similarly for the odd case. Ranks: |R^{2j}λ| = n − j,
|R^{2j+1}λ| = n − j. The second half of the orbit by R^{−k}λ = −R^{k+1}λ:
rank(R^{2n−k}λ) = n − rank(R^{k+1}λ). The shift: for even n and 0 ≤ i ≤ n−1,
rank(R^{i+n}λ) − rank(R^iλ) = −n/2 for i ≥ 1 (i = 2j or 2j+1: the first term is
⌊(n − 2j + 1)/2⌋ = n/2 − j for j ≥ 1, and n/2 for i = 1, against n − j) and
+n/2 for i = 0; the other half follows since H is an involution. ∎

**Proposition BF-C (w_n on the orbit of λ; n even).** w_n preserves the orbit
O₁ of λ and acts on the clock positions by i ↦ 2 − i for even i and i ↦ −i
for odd i (mod 2n). Consequently w_n commutes with H = Rⁿ on O₁.

*Proof.* For 1 ≤ j ≤ n/2, R^{2j}λ = [n] ∖ E_j with E_j = {n−2j+2, …, n} (even
positions); π fixes n and maps every other even position 2k to 2k+1, so
π(R^{2j}λ) = [n] ∖ ({n} ∪ O_j) with O_j = {n−2j+3, …, n−1} (odd), and
w_n(R^{2j}λ) = π(·) Δ [n−1] = O_j. But O_j = F_{j−1} := {n−2(j−1)+1, …, n−1}
and R^{2(j−1)+1}λ = [n] ∖ F_{j−1}, so O_j = −R^{2j−1}λ = R^{2−2j}λ by the
antipode identity. For j = 0, w_n(λ) = [n−1] = R²λ (Proposition BF-B), which
is the same rule. For 0 ≤ j ≤ n/2 − 1, R^{2j+1}λ = [n] ∖ F_j with F_j odd
positions ≥ 3, so π(F_j) = {n−2j, …, n−2} and w_n(R^{2j+1}λ) = π(F_j) ∪ {n} =
E_{j+1} = −R^{2j+2}λ = R^{−(2j+1)}λ. This covers positions 0..n+1; the rest
follow from w_n(−y) = −w_n(y) and R^{−k}λ = −R^{k+1}λ. Both maps i ↦ 2 − i
and i ↦ −i commute with i ↦ i + n when n is even (parity is preserved by
adding n), so w_n H = H w_n on O₁. ∎

**Lemma BF-D (carrying orbits).** If H agrees with an involution w on an
orbit O (H|_O = w|_O), then wH = Hw on O. *Proof.* For x ∈ O, wHx = wwx = x
and Hwx = HHx = x. ∎

**Proposition BF-E (two half-turn values; n even ≥ 6).**
(i) H(e₂) = {1, 2} ∪ {5, 7, …, n−1}, where e₂ = {2}.
(ii) H({3, n}) = [n] ∖ {4, 6, 7, 9, 11, …, n−1} for n ≥ 8, and H({3, 6}) =
{1, 2, 3, 5, 6}. In every case 2 ∈ H({3, n}) and 3 ∉ H(e₂).

*Proof of (i).* Excess sequences, by Lemma BF-A. e₂ has plus positions
[n] ∖ {2}: d = (0, 1^{n−2}), r = n−1, G = {2}, no cap: R e₂ has d = (0, 0)
(rows ≥ 3 absent). Then no gap rows, cap = n − 3: R²e₂ = (n−3)³. Then G = {1},
no cap: R³e₂ = (n−4). Then G = {1} (n ≥ 5), cap = n − 2: R⁴e₂ = (n−5, n−2).
Claim: for 2 ≤ j ≤ n/2 − 1, R^{2j}e₂ = (n−1−2j, n−2j+2, n−2j+3, …, n−j) (length
j) and R^{2j+1}e₂ = (n−2−2j, n−2j+1, …, n−j−1) (length j). From R^{2j}: all
rows are gap rows (first entry ≥ 1 as j ≤ n/2 − 1; the run rises by 3 then by
1), d_j = n − j = n − r, no cap: subtract 1 everywhere — the odd formula. From
R^{2j+1} with j ≤ n/2 − 2: all gap rows, d_j = n−j−1 < n−j, cap = n−j−1:
d′_k = d_k − 1 (each ≤ n−j−2 < cap) and d′_{j+1} = cap: (n−3−2j, n−2j, …,
n−j−1) — the even formula for j+1. At j = n/2 − 1 the odd formula reads
(0, 3, 4, …, n/2) (length n/2 − 1): row 1 is not a gap row, rows 2.. are,
d_r = n/2 < n/2 + 1 = n − r so cap = n/2: d′₁ = min(d₂ − 1, cap) = 2,
d′_k = d_k − 1 for 2 ≤ k ≤ r, d′_{r+1} = n/2, i.e. H(e₂) = Rⁿe₂ has
d = (2, 2, 3, 4, …, n/2), plus positions c_k = d_k + k = (3, 4, 6, 8, …, n),
minus set {1, 2} ∪ {5, 7, …, n−1}. ∎

*Proof of (ii).* {3, n} has plus positions [n] ∖ {3, n}: d = (0, 0, 1^{n−4}),
r = n−2, G = {3}, cap = 1 present (d_r = 1 < 2): R{3,n} = (0, 0, 0, 1^{n−4})
= e₄. Then G = {4}, no cap: (0,0,0,0). Then no gap rows, cap = n−5: (n−5)⁵.
Then G = {1}, no cap: (n−6). For n = 6 this is (0): no gap row, cap = n−2 = 4
gives R⁵{3,6} = (4, 4), then G = {1}, no cap, gives H{3,6} = (3): plus
position 4, minus set {1,2,3,5,6}. For n ≥ 8: G = {1}, cap = n−2:
R⁵{3,n} = (n−7, n−2). Claim: for 2 ≤ j ≤ n/2 − 2, R^{2j+1}{3,n} = (n−3−2j,
n−2j+2, …, n−j) (length j) and R^{2j+2}{3,n} = (n−4−2j, n−2j+1, …, n−j−1)
(length j). From R^{2j+1}: all gap rows (first entry ≥ 1), d_r = n−j = n − r,
no cap: subtract 1 — the next formula. From R^{2j+2} with j ≤ n/2 − 3: all
gap rows, d_r = n−j−1 < n−j, cap = n−j−1: d′_k = d_k − 1 and d′_{j+1} = cap:
(n−5−2j, n−2j, …, n−j−1) — the formula for j+1. At j = n/2 − 2 the even
formula is R^{n−2}{3,n} = (0, 5, 6, …, n/2+1) (length n/2 − 2): row 1 not a
gap row, cap = n/2 + 1 present: R^{n−1}{3,n} = (4, 4, 5, …, n/2+1) (length
n/2 − 1). Then G = {1, 3, 4, …, r}, d_r = n/2 + 1 = n − r, no cap: d′₁ = 3,
d′₂ = d₃ − 1 = 4, d′_k = d_k − 1 = k + 1 for k ≥ 3: H{3,n} has d = (3, 4, 4,
5, …, n/2), plus positions (4, 6, 7, 9, 11, …, n−1), minus set as stated;
2 is a minus position. ∎

**Theorem BF (why four).** Let n ≥ 4 be even. Then w_n commutes with the
half-turn Rⁿ if and only if n = 4.

*Proof.* n = 4: the board is O₁ ∪ O₂ (SM-060); w₄ = −τ commutes with R⁴ on O₁
by Proposition BF-C and on O₂ by Lemma BF-D, since R⁴ = −τ there (SM-063).
n ≥ 6: take x = e₂. By BF-E(i), 3 ∉ H(e₂), so 2 ∉ π(H(e₂)) (π swaps 2 and 3),
so 2 ∈ w_n(H e₂) = π(H e₂) Δ [n−1]. On the other side w_n(e₂) = π(e₂) Δ [n−1]
= [n−1] ∖ {3} = −{3, n}, so H(w_n e₂) = −H({3, n}) since H commutes with −1;
by BF-E(ii) 2 ∈ H({3, n}), hence 2 ∉ H(w_n e₂). So w_n H e₂ ≠ H w_n e₂. ∎

**Corollary BF (with SM-068).** On the free spinor clocks the criterion's
positive case occurs only at n = 4: for even n ≥ 6 the correction
c = w_n⁻¹Rⁿ is not an involution (Lemma BD-A), and w_n cannot be an extra
half-turn survivor. What remains unproved is that w_n is the *nearest* Weyl
element for n > 8 and that C_W(c) collapses to {±1} in general (computed for
n = 6, 7, 8, 16).

## 3. Method (compute, never assert)

1. Implement Lemma BF-A as a function on excess sequences and check it
   against the combinatorial clock of SM-069 on every weight for n = 4..12
   (BF1) — the lemma's proof is above; this is its independent check.
2. Check Propositions BF-B, BF-C, BF-E and Theorem BF on the boards n = 4..16
   (even) and BF-B also on odd n ≤ 15, on the actual clock (BF2–BF5).
3. Record the exploration's two observations as [obs] with their range.

## 4. Bars and registered guesses

- **BF0** lock. **BF1 [C]** Lemma BF-A = the clock, n = 4..12, all 2ⁿ weights.
- **BF2 [C]** Prop. BF-B on n = 3..16 (ranks and the sets; the ±n/2 shift for
  even n). **BF3 [C]** Prop. BF-C on even n = 4..16 (the two reflections; the
  commutation on O₁). **BF4 [C]** Prop. BF-E on even n = 6..16 (both sets).
  **BF5 [C]** Theorem BF: e₂ is a witness for even n = 6..16 and w₄ ∈ C₄.
- **BF6 [obs, registered guess].** R³(e_p) = −e_{n−1−p} for 1 ≤ p ≤ n−2, and
  H(e_p) = {1,2} ∪ {4, …, 2m} ∪ {2m′+1, …, n−1} with m = p−1, m′ = p for
  p ≤ n/2 and m = m′ = n−p for p ≥ n/2 (seen for n = 6..14 in the exploration;
  guess: holds for n = 16 too). Not used in the proof.
- **BF7 [obs, registered guess].** For odd n = 5, 7 the element found nearest in
  SM-068 (−(e₂e₃)…, the last two coordinates negated) does not commute with
  Rⁿ; guess: the same for n = 9, 11 with the analogous element. Not claimed
  as a theorem.

## 5. Not claimed

That w_n is the nearest Weyl element to Rⁿ for n > 8; that C_W(w_n⁻¹Rⁿ) =
{±1} for all even n ≥ 6 (computed at n = 6, 8, 16); anything about odd n
beyond BF7's computations; the closed form of BF6 as a theorem; nothing
physical.

## 6. Files

`verify_stone_bf_why_four.py`, `.log`, `_FIRSTRUN.log`,
`STONE_BF_WHY_FOUR.md`; the exploration `_explore_bf_witness_2026-09-19.py`
+ `.log` (kept, not sealed). Registry row SM-070 on seal; paper edits (v2.0
§6.4 Theorem, v1.0 §7.12.4) in the same seal; packaging for Ilya on
Selina's word.

# zips/ — the send shelf

Everything READY TO SEND to Ilya sits here: one zip + its COVER sha file
per envelope. Grab the zip, attach it, quote the sha from the cover file
so he can verify on arrival.

House rules:
- `package/` remains the sealed archive of record (canonical hashes,
  never edited after a send is recorded). This folder is a convenience
  MIRROR for Selina's hand — same bytes, same hashes.
- A send is recorded in `SCAR_MERKABIT_REGISTRY.md` (Sends section) only
  on Selina's word; after that the envelope is FROZEN.

Currently on the shelf (2026-08-30):
- `Clifford_Trilogy_2026-08-30.zip`  (sha in COVER_TRILOGY_SHA256.txt)
- `One_Law_Reply_2026-08-30.zip`     (sha in COVER_ONELAW_SHA256.txt)
